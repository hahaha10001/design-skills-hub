# Normalise to LF in the repository regardless of contributor OS. CI runs the
# gate chain on ubuntu-latest, where a CRLF-terminated shell step fails.
* text=auto eol=lf

*.png binary
*.jpg binary
*.skill binary
